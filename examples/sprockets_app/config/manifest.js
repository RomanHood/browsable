// Sprockets manifest. browsable does NOT parse this file — runtime mode reads
// what Rails actually renders, which is the authoritative answer for "which
// assets ship?". Included only so the fixture matches a real Sprockets layout.
//= link_tree ../images
//= link_directory ../stylesheets .css
//= link application.js
